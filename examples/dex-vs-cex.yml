# Test configuration for monitoring
http_api:
  - name: TestAPI
    target: "https://httpbin.org/json"
    interval: "s30"
    fields:
      - name: "test_value"
        selector: ".slideshow.slides[0].title"
        type: "string"
